add_subdirectory(cli)
add_subdirectory(loader)

if(RPLC_XL)
    if(NOT RHS_SERVICE_NOTIFICATION_DISABLE)
        set(RHS_SERVICE_NOTIFICATION ON)
    endif()
    if(NOT RHS_SERVICE_LWIP_DISABLE)
        set(RHS_SERVICE_LWIP ON)
    endif()
    if(NOT RHS_CAN_OPEN_DISABLE)
        set(RHS_CAN_OPEN ON)
    endif()
elseif(RPLC_L)
    if(NOT RHS_SERVICE_NOTIFICATION_DISABLE)
        set(RHS_SERVICE_NOTIFICATION ON)
    endif()
    if(NOT RHS_CAN_OPEN_DISABLE)
        set(RHS_CAN_OPEN ON)
    endif()
elseif(RPLC_M)
    if(NOT RHS_SERVICE_NOTIFICATION_DISABLE)
        set(RHS_SERVICE_NOTIFICATION ON)
    endif()
    if(NOT USB_SERIAL_BRIDGE_DISABLE)
        set(RHS_SERVICE_USB_BRIDGE ON)
    endif()
    if(NOT RHS_CAN_OPEN_DISABLE)
        set(RHS_CAN_OPEN ON)
    endif()
endif()

message("\tSERVICES:")
if(RHS_SERVICE_NOTIFICATION)
    message("\t\tRHS_SERVICE_NOTIFICATION\t- ON")
    add_subdirectory(notification)
else()
    message("\t\tRHS_SERVICE_NOTIFICATION\t- OFF")
endif()
if(RHS_SERVICE_LWIP)
    message("\t\tRHS_SERVICE_LWIP\t\t\t- ON")
    add_subdirectory(LWIP)
else()
    message("\t\tRHS_SERVICE_LWIP\t\t\t- OFF")
endif()
if(RHS_SERVICE_USB_BRIDGE)
    message("\t\tRHS_SERVICE_USB_BRIDGE\t\t- ON")
    add_subdirectory(usb_serial_bridge)
else()
    message("\t\tRHS_SERVICE_USB_BRIDGE\t\t- OFF")
endif()
if(RHS_CAN_OPEN)
    message("\t\tRHS_CAN_OPEN\t\t\t\t- ON")
    add_subdirectory(can_open)
else()
    message("\t\tRHS_CAN_OPEN\t\t\t\t- OFF")
endif()
